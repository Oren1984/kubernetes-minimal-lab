# Helm Chart metadata for Kubernetes minimal lab
# Defines chart name, description, version, and app version

apiVersion: v2
name: kubernetes-minimal
description: A minimal Helm chart for a Flask web app
type: application
version: 0.1.0
appVersion: "1.0.0"
